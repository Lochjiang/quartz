---
tags: Python
---
### 线程池

```python
from concurrent.futures import ThreadPoolExecutor

with ThreadPoolExecutor(max_workers=1) as t:  # 创建一个最大容纳数量为5的线程池
    # tasks = [func(i) for i in range(len(df))]  # 创建任务列表
    # wait(tasks,return_when=FIRST_COMPLETED)
    tasks = [t.submit(func, i) for i in range(len(df))]

df.to_excel('test.xlsx')
```