<%*
const TYPES = [
	"健身",
];

/* 下拉选择，两个参数，一个是显示，一个是实际*/
let type = await tp.system.suggester(TYPES,TYPES,false,"选择词典分类");
/* ⭐ 如果按ESC -> 直接终止模板 */
if (!type) {
    new Notice("已取消插入"); // 可选提示
    return;
}

tR += `---\ntags: ${type}\n---\n`;
/* 重名检测 */  
if (app.vault.getAbstractFileByPath(`词典/${type}/${tp.file.title}.md`)) {  
new Notice("⚠词典已存在同名文件/本文件");  
} 
else {  
await tp.file.move(`词典/${type}/${tp.file.title}`);  
}
%>