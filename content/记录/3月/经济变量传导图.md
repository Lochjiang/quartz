---
tags:
  - 记录
---
### 代码

```python
# %%
import pandas as pd
import networkx as nx
import sympy as sp

β = sp.symbols('β')
α = sp.symbols('α')
e_Lr = sp.symbols('e_Lr')
e_NW = sp.symbols('e_NW')
e_Ly = sp.symbols('e_Ly')

e_rP = 1/e_Lr
e_NP = e_NW/((1 - α)*e_NW+1)
e_yN = α
e_ry = e_Ly/e_Lr
e_ir = -1/(1-β)
e_Ki = 1
e_yK = β
e_sy = 1

# %%
variables = ['P','r','N','y','i','K','s']

df = pd.DataFrame([], index=variables, columns=variables)

df.loc['P','r'] = e_rP
df.loc['P','N'] = e_NP 
df.loc['N','y'] = e_yN
df.loc['y','r'] = e_ry
df.loc['r','i'] = e_ir
df.loc['i','K'] = e_Ki
df.loc['K','y'] = e_yK
df.loc['y','s'] = e_sy
df.fillna(0,inplace=True)


# %%
A = sp.Matrix(df.to_numpy())
I = sp.eye(len(variables))
B = (I - A)**(-1)-I

# %%
B[0,6]/B[0,4]
```

### 方法2

```python
# %%
import networkx as nx
import sympy as sp 
import pandas as pd

class EconomicSystem(nx.DiGraph):
    def __init__(self):
        super().__init__()

    def get_B(self):
        df = nx.to_pandas_adjacency(self,dtype=object)
        A = sp.Matrix(df)   
        I = sp.eye(A.shape[0])
        B = (I-A).inv()
        mat = pd.DataFrame(B.tolist(),index=df.index,columns=df.columns)
        return mat
    
    def get_A(self):
        df = nx.to_pandas_adjacency(self,dtype=object)
        return df

    # def set_rational_var(self,var,target1,target2):
e_Lr = sp.symbols('e_Lr',positive=True)
α = sp.symbols('α',positive=True)
β = sp.symbols('β',positive=True)
e_NW = sp.symbols('e_NW',positive=True)
e_Ly = sp.symbols('e_Ly',positive=True)

# %%
G = EconomicSystem()
G.add_weighted_edges_from([
    ('P','m_e',-1),
    ('P','VMP_e',1),
    ('VMP_e','VMP',1),
    ('VMP_e','W',1/((1-α)*e_NW+1)),
    ('m_e','m',1),
    ('m_e','r',-1/e_Lr),
    ('r','i',-1/(1-β)),
    ('r','L',-e_Lr),
    ('W','N',e_NW),
    ('N','VMP',-(1-α)),
    ('N','y',α),
    ('y','Le',e_Ly),
    ('Le','L',1),
    ('Le','r',1/e_Lr),
    ('y','s',1),
    ('i','K',1),
    ('K','MP_K',-(1-β)),
    ('K','y',β)

])

df = G.get_B()
```

## 极简版代码

```python
# %%
import networkx as ntwx
import sympy as sp 
import pandas as pd

class EconomicSystem(ntwx.DiGraph):
    def __init__(self):
        super().__init__()

    def get_B(self):
        df = ntwx.to_pandas_adjacency(self,dtype=object)
        A = sp.Matrix(df)   
        I = sp.eye(A.shape[0])
        B = (I-A).inv()
        mat = pd.DataFrame(B.tolist(),index=df.index,columns=df.columns)
        return mat
    
    def get_A(self):
        df = ntwx.to_pandas_adjacency(self,dtype=object)
        return df

    # def set_rational_var(self,var,target1,target2):
e_Lr = sp.symbols('e_Lr',positive=True)
α = sp.symbols('α',positive=True)
β = sp.symbols('β',positive=True)
e_NW = sp.symbols('e_NW',positive=True)
e_Ly = sp.symbols('e_Ly',positive=True)
i,g,nx = sp.symbols('i g nx',positive = True)


i = sp.IndexedBase('i')
s = sp.IndexedBase('s')
D = sp.IndexedBase('D')
r = sp.IndexedBase('r')
y = sp.IndexedBase('y')
m = sp.IndexedBase('m')
P = sp.IndexedBase('P')
M = sp.IndexedBase('M')

# %%
G = EconomicSystem()
G.add_weighted_edges_from([
    (P,y[P],α*e_NW/((1-α)*e_NW+1)),
    (P,m[P],-1),
    (m[P],r[P],-1/e_Lr),
    (r[P],i[P],-1/(1-β)),
    (i[P],y[P],β),
    (i[P],D[P],1),
    (y[P],s[P],1),
    (y[P],r[P],e_Ly/e_Lr),
    # M部分
    (M,m[M],1),
    (m[M],r[M],-1/e_Lr),
    (r[M],i[M],-1/(1-β)),
    # (i[M],P),
    (i[M],y[M],β),
    (i[M],D['E'],1),
    (y[M],r[M],e_Ly/e_Lr),
    (y[M],s[M],1),
    #加总
    (i[M],i,1),
    (i[P],i,1),
    (s[M],s,1),
    (s[P],s,1),
    (y[M],y,1),
    (y[P],y,1),
    (r[M],r,1),
    (r[P],r,1),
    (m[M],m,1),
    (m[P],m,1), 
    (D['E'],D,1),
    (D[P],D,1)
    #政府、国际部门
    
])

df = G.get_B()
```

### 古典-凯恩斯系数

$$C-K = \frac{-e_{L r}e_{NW}\alpha\beta+e_{L r}e_{NW}\alpha+e_{N W}\alpha\beta-e_{N W}\beta-\beta}{-e_{L y}e_{NW}\alpha+e_{N W}\alpha-e_{N W}-1}$$

### 笔记

- 提取内生入边
- 提取外生入边
- 提取内生入边上的外生支线
	- 判断没有入边的变量
	- 该变量到target不包含agent的支线
	- 复刻支线上的所有变量和出边
	- 改道至外生变量
---
出边
内生入边

内生路径上的外生入边
外生入边
## 构造

### 附带M

```python
G.add_weighted_edges_from([
    ('P','m_e',-1),
    ('P','VMP_e',1),
    ('VMP_e','VMP',1),
    ('VMP_e','W',1/((1-α)*e_NW+1)),
    ('m_e','m',1),
    ('m_e','r',-1/e_Lr),
    ('r','i',-1/(1-β)),
    ('r','L',-e_Lr),
    ('W','N',e_NW),
    ('N','VMP',-(1-α)),
    ('N','y',α),
    ('y','Le',e_Ly),
    ('Le','L',1),
    ('Le','r',1/e_Lr),
    ('y','s',1),
    ('i','K',1),
    ('i','D',1),
    ('D_e','D',1),
    ('K','MP_K',-(1-β)),
    ('K','y',β),
    ('M','m_M',1),
    ('m_M','m',1),
    ('m_M','r_M',-1/e_Lr),
    ('r_M','L',-e_Lr),
    ('r_M','i_M',-1/(1-β)),
    ('i_M','K',1),
    ('i_M','D_e',1),
    ('s_e','s',1)
])
```

### 无反馈路径

```python
G.add_weighted_edges_from([
    ('P','m',-1),
    ('P','VMP',1),
    ('r','L',-e_Lr),
    ('M','m',1),
    ('y','L',e_Ly),
    ('W','N',e_NW),
    ('N','VMP',-(1-α)),
    ('N','y',α),
    ('y','s',1),
    ('i','K',1),
    ('K','MP_K',-(1-β)),
    ('K','y',β)
])
```

## 无权重

```python
import networkx as nx
import sympy as sp 
import pandas as pd

class EconomicSystem(nx.DiGraph):
    def __init__(self):
        super().__init__()

    def get_B(self):
        df = nx.to_pandas_adjacency(self,dtype=object)
        A = sp.Matrix(df)   
        I = sp.eye(A.shape[0])
        B = (I-A).inv()
        mat = pd.DataFrame(B.tolist(),index=df.index,columns=df.columns)
        return mat
    
    def get_A(self):
        df = nx.to_pandas_adjacency(self,dtype=object)
        return df

    # def set_rational_var(self,var,target1,target2):
e_Lr = sp.symbols('e_Lr',positive=True)
e_NW = sp.symbols('e_NW',positive=True)
e_Ly = sp.symbols('e_Ly',positive=True)
α = sp.symbols('α',positive=True)
β = sp.symbols('β',positive=True)

P = sp.IndexedBase('P')
S = sp.IndexedBase('S')
D = sp.IndexedBase('D')

G = EconomicSystem()
G.add_weighted_edges_from([
    (P,S,α),
    (P,D,-β )
])

# df = G.get_B()
```

## 三部门精简

```python
# %%
import networkx as nx
import sympy as sp 
import pandas as pd

class EconomicSystem(nx.DiGraph):
    def __init__(self):
        super().__init__()

    def get_B(self):
        df = nx.to_pandas_adjacency(self,dtype=object)
        A = sp.Matrix(df)   
        I = sp.eye(A.shape[0])
        B = (I-A).inv()
        mat = pd.DataFrame(B.tolist(),index=df.index,columns=df.columns)
        return mat
    
    def get_A(self):
        df = nx.to_pandas_adjacency(self,dtype=object)
        return df

    # def set_rational_var(self,var,target1,target2):
e_Lr = sp.symbols('e_Lr',positive=True)
α = sp.symbols('α',positive=True)
β = sp.symbols('β',positive=True)
e_NW = sp.symbols('e_NW',positive=True)
e_Ly = sp.symbols('e_Ly',positive=True)

i = sp.IndexedBase('i')
s = sp.IndexedBase('s')
D = sp.IndexedBase('D')
r = sp.IndexedBase('r')
y = sp.IndexedBase('y')
m = sp.IndexedBase('m')
P = sp.IndexedBase('P')
M = sp.IndexedBase('M')

# %%
G = EconomicSystem()
G.add_weighted_edges_from([
    (P,y[P],α*e_NW/((1-α)*e_NW+1)),
    (P,m[P],-1),
    (m[P],r[P],-1/e_Lr),
    (r[P],i[P],-1/(1-β)),
    (i[P],y[P],β),
    (i[P],D[P],1),
    (y[P],s[P],1),
    (y[P],r[P],e_Ly/e_Lr),
    # M部分
    (M,m[M],1),
    (m[M],r[M],-1/e_Lr),
    (r[M],i[M],-1/(1-β)),
    # (i[M],P),
    (i[M],y[M],β),
    (i[M],D[M],1),
    (y[M],r[M],e_Ly/e_Lr),
    (y[M],s[M],1),
    #加总
    (i[M],i,1),
    (i[P],i,1),
    (s[M],s,1),
    (s[P],s,1),
    (y[M],y,1),
    (y[P],y,1),
    (r[M],r,1),
    (r[P],r,1),
    (m[M],m,1),
    (m[P],m,1), 
    (D[M],D,1),
    (D[P],D,1)
])

df = G.get_B()
```